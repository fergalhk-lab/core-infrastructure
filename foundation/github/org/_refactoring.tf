# Delete this file after the import has been applied.

import {
  to = github_repository.repos["core-infrastructure"]
  id = "core-infrastructure"
}

import {
  to = github_repository.repos["apps"]
  id = "apps"
}

import {
  to = github_repository.repos["deployer"]
  id = "deployer"
}
